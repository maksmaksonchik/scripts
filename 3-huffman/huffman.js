const { getCodeMap } = require('./src/get-code-map');
const { getCodeTree } = require('./src/get-code-tree');

const codeMap = getCodeMap('');

console.log(codeMap);

// const codeTree = getCodeTree('');
//
// console.log(codeTree);
